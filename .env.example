# X/Twitter API credentials
# Get from https://developer.twitter.com/
X_BEARER_TOKEN=your_bearer_token_here

# OpenAI API
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview
# Max tokens for completion (increase for reasoning models like gpt-5/o1)
OPENAI_MAX_COMPLETION_TOKENS=16000
# Temperature for sampling (leave empty for model default, required for reasoning models)
# OPENAI_TEMPERATURE=0.7

# Telegram Bot
# Create bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
EMAIL_TO=recipient@example.com

# Schedule (cron format, default: 8:00 AM daily)
SUMMARY_CRON_HOUR=8
SUMMARY_CRON_MINUTE=0

# Database
DATABASE_PATH=data/x_monitor.db

# Rate Limiting
# When rate limit is hit, the scraper will skip the request and continue with next account
# Delay between each account request (seconds)
RATE_LIMIT_DELAY=15.0
# Number of accounts to process before taking a longer break
RATE_LIMIT_BATCH_SIZE=3
# Delay between batches (seconds) - 3 minutes break
RATE_LIMIT_BATCH_DELAY=180.0
# [DEPRECATED] Maximum retries when rate limited (no longer used, kept for compatibility)
RATE_LIMIT_MAX_RETRIES=5
# [DEPRECATED] Base delay for exponential backoff (no longer used, kept for compatibility)
RATE_LIMIT_RETRY_BASE_DELAY=120.0
